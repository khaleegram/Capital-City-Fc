rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read-only access to player profiles
    // Admins can create, update, and delete profiles
    match /players/{playerId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
